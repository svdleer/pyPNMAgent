# PyPNM Agent - Python Dependencies
# Install on Jump Server
# Note: pysnmp>=7 requires Python 3.9+
#       pysnmp 6.x works on Python 3.8 — agent auto-detects which version to use

websocket-client>=1.6.0
paramiko>=3.3.0
redis>=5.0.0
pysnmp>=6.2.6  # 6.x for Python 3.8, 7.x for Python 3.9+
pysnmp-mibs>=0.1.6
icmplib>=3.0
