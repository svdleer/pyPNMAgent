__pycache__/
*.py[cod]
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/
*.tar.gz

# Config files with secrets
agent_config.json
agent_config.cmts-agent.json
agent_config.cm-agent.json
agent_config.modem-agent.json
config/

# Virtual environments
venv/
.venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
logs/

# SSH keys
*.pem
id_*
# Committed DOCSIS MIBs for agent SNMP operations

# local env overrides (keep secrets out of git)
.env

# local tunnel secrets — never commit
.env

# deploy script with real host details — keep example in scripts/
deploy/push-agent.sh
